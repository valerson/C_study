nt n = strlen(s);
            // for (int i = n - 1; i >= n - 4; i -= 1)
            // {

            //     s[i - 4] = s[i];
            //     if (i == n - 4)
            //         s[i] = '\0';
            // }